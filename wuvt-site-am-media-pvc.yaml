kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: wuvt-site-am-media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  selector:
    matchLabels:
      app: wuvt-site
      instance: am
      mount: media
